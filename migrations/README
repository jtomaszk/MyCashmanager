Generic single-database configuration.


For initialize migrations
python app.py db init

For create a new migration
python app.py db migrate -m "migration info"

To run migrations
python app.py db upgrade
